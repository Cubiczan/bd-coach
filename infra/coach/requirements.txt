# Live coach service. Deliberately small — transcription and inference are
# delegated to containers the stack already runs.
#
# FastAPI 0.134.0 is the first release that accepts Starlette 1.0.1+
# (GHSA-86qp-5c8j-p5mr Host-header poison). Starlette 1.0.1 also includes
# the Range-header DoS fix from 0.49.1 (CVE-2025-62727). Do not drop the
# explicit starlette pin — FastAPI's range is >=0.46.0 with no upper bound.
fastapi==0.134.0
starlette==1.0.1
uvicorn[standard]==0.34.0
httpx==0.28.1
pyyaml==6.0.2
